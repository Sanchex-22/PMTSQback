# Ejemplo de contenido en wrangler.toml
name = "mi-worker-cloudflare"
main = "dist/worker.js" # O la ruta a tu script de worker ya compilado
compatibility_date = "2024-06-17" # Actualiza esta fecha

[build]
command = "npm run build:worker" # <--- Esta es la línea que causa el problema
# watch_dir = "src" # Opcional, directorio con el código fuente del worker